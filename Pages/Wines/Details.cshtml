@page
@model LiqourCentre.Pages.Wines.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Wine</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Wine.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Wine.name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Wine.price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Wine.price)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Wine.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
